import React from "react";
import Header from "../components/Header";

const Home = (props) => {
   return (
      <div>
         <Header from={"home"} title={"home"} />
         <div>
            <h1>Home Pages</h1>
         </div>
      </div>
   );
};

export default Home;
